var number = 19.6

var newValue = number.toString()

var newValue = Math.trunc(number)  // baixo

var newValue = Math.cell(number) // cima 

var newValue = Math.round(number) // para o mais próximo

console.log(number)